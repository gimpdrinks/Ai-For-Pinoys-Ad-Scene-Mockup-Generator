export interface GeneratedAdContent {
  id: string;
  imageUrl: string | null;
  text: string | null;
}

export interface Scene {
  id: string;
  name:string;
  thumbnail: string;
  description: string;
}